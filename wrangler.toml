# lean-landing → Cloudflare Pages（純前端一頁式免費部署）
# 部署：npm run deploy（＝vite build && wrangler pages deploy dist）
# 玩家亮相時教練代打；帳號只辦 CF（不碰 GitHub，一次一道註冊牆）。
name = "lean-landing"
pages_build_output_dir = "dist"
compatibility_date = "2026-07-23"
